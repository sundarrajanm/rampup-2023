{
  "name": "goserverlesslambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "env GOOS=linux GOARCH=amd64  go build  -o bin/goserverlesslambda .",
    "deploy:dev": "serverless deploy --aws-profile default",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:deploy": "npm run build && npm run deploy:dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "env-cmd": "^10.1.0",
    "serverless-dotenv-plugin": "^4.0.2"
  }
}
